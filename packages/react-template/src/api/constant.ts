export enum Method {
  get = 'GET',
  post = 'POST',
  put = 'PUT',
  patch = 'PATCH',
  delete = 'DELETE',
}

export enum Header {
  contentType = 'content-type',
}

export enum MimeType {
  json = 'application/json',
}
